# @hiworld/client

React client for Hey World.

## Scripts

Run from the repo root:

```
pnpm --filter @hiworld/client dev
pnpm --filter @hiworld/client build
pnpm --filter @hiworld/client test
```

The development server runs at `http://localhost:3000`.
